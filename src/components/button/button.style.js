import { COLORS, FONT_SIZE } from "../../constants/theme.js"
export const styles = {

    btn: {
        width: "100%",       
        backgroundColor: COLORS.green,
        padding: 12,
        marginTop: 600,
        borderRadius: 30
},

    text: {
        color: "white",
        fontSize: FONT_SIZE.grande,
        textAlign: "center"
    }
}